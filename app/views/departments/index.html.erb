<p id="notice"><%= notice %></p>

<h1>Departamentos</h1>

<table>
  <thead>
    <tr>
      <th>Código de Departamento</th>
      <th>Descripción</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @departments.each do |department| %>
      <tr>
        <td><%= department.num_depto %></td>
        <td><%= department.description %></td>
        <td><%= link_to 'Mostar', department %></td>
        <td><%= link_to 'Agregar Municipio', municipalities_path(:department => department) %></td>
        <td><%= link_to 'Modificar', edit_department_path(department) %></td>
        <td><%= link_to 'Eliminar', department, method: :delete, data: { confirm: 'Esta Seguro?' } %></t>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nuevo Departamento', new_department_path %>
