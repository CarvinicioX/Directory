<%= form_for(municipality) do |f| %>
  <% if municipality.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(municipality.errors.count, "error") %> prohibited this municipality from being saved:</h2>

      <ul>
      <% municipality.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field" style="visible:false">
    <%= f.text_field :Department_id, :value => params[:kk], :hidden => true %>
  </div>

  <div class="field">
    <%= f.label :"Código de Municipio" %>
    <%= f.text_field :num_municip %>
  </div>

  <div class="field">
    <%= f.label :"Descripción" %>
    <%= f.text_field :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
